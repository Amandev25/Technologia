// API Configuration
export const API_BASE_URL = 'http://10.0.0.237:3000/api/v1/ai';

// Using your Wi-Fi IP address for device/emulator connectivity
// Make sure your backend is running and both devices are on the same WiFi network
